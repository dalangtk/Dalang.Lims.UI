# Dalang.Lims.UI

#### 📖介绍
Dalang LIMS 是一个基于 Vue 3 + TypeScript + element plus + Vite 构建的实验室信息管理系统（LIMS）前端项目。

> Vite 不再支持 Node 12 / 13 / 15，因为上述版本已经进入了 EOL 阶段。建议使用 Node 16+ 版本。

```bash
# 安装依赖
npm install

# 运行项目
npm run dev

# 打包发布
npm run build
```